<UserControl x:Class="OtterWorks.Desktop.Views.DocumentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Documents" FontSize="22" FontWeight="Bold" Foreground="#111827" />
                <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource MutedBrush}"
                           VerticalAlignment="Bottom" Margin="12,0,0,3" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="0,0,8,0"
                        Padding="12,6" Background="White" BorderBrush="#D1D5DB" BorderThickness="1" Cursor="Hand" />
                <Button Content="Files" Command="{Binding LoadFilesCommand}" Margin="0,0,8,0"
                        Padding="12,6" Background="White" BorderBrush="#D1D5DB" BorderThickness="1" Cursor="Hand" />
                <Button Content="Log out" Command="{Binding LogoutCommand}"
                        Padding="12,6" Background="White" BorderBrush="#D1D5DB" BorderThickness="1" Cursor="Hand" />
            </StackPanel>
        </Grid>

        <!-- Create row -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="12" BorderBrush="#E5E7EB"
                BorderThickness="1" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Padding="8,6" FontSize="14" VerticalContentAlignment="Center"
                         Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}"
                         Tag="New document title" />
                <Button Grid.Column="1" Content="New" Style="{StaticResource PrimaryButton}"
                        Command="{Binding CreateCommand}" Margin="10,0,0,0" MinWidth="90" />
            </Grid>
        </Border>

        <!-- List + states -->
        <Grid Grid.Row="2">
            <Border Background="White" CornerRadius="8" BorderBrush="#E5E7EB" BorderThickness="1">
                <ListBox ItemsSource="{Binding Documents}" BorderThickness="0" Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="12,10" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="SemiBold"
                                               Foreground="#111827" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,2,0,0">
                                        <Run Text="{Binding WordCount, Mode=OneWay}" />
                                        <Run Text="words · v" />
                                        <Run Text="{Binding Version, Mode=OneWay}" />
                                    </TextBlock>
                                </StackPanel>
                                <TextBlock Grid.Column="1" Text="{Binding UpdatedAt, StringFormat=g}"
                                           Foreground="{StaticResource MutedBrush}" FontSize="12" VerticalAlignment="Center" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <!-- Empty state -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                        Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="No documents yet" FontSize="16" FontWeight="SemiBold"
                           Foreground="#374151" HorizontalAlignment="Center" />
                <TextBlock Text="Create your first document using the box above."
                           Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0" HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>

        <!-- Footer: error + busy -->
        <StackPanel Grid.Row="3" Margin="0,12,0,0">
            <ProgressBar IsIndeterminate="True" Height="3" BorderThickness="0"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}" TextWrapping="Wrap"
                       Margin="0,8,0,0"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibility}}" />
        </StackPanel>
    </Grid>
</UserControl>
